---
- hosts: local

  tasks:

    - name: Remove old container
      shell: docker rm -f myapp || true

    - name: Build docker image
      shell: docker build -t myapp .

    - name: Run docker container
      shell: docker run -d --name myapp -p 80:80 myapp
